import { Grid } from "semantic-ui-react"

interface VinylData {
    id: number,
    year: number,
    link: string,
    artist: string,
    title: string,
    price: number
}

export const ColumnDisplay = (props: VinylData) => {


    return (
        <Grid>
            
        </Grid>
    )
}